from typing import List, Dict, Union

def format_variations(variations: List[Dict[str, Union[str, int]]]) -> List[Dict[str, Union[str, int]]]:
    """
    Normalizes the output variations from different AI providers.
    Currently, providers are expected to return a standard format.
    """
    return variations
