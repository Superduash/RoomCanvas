from PIL import Image

def preprocess_image(image: Image.Image) -> Image.Image:
    """
    Applies any necessary preprocessing to the input image before it is sent to the AI provider.
    e.g. resizing, compression, normalization.
    """
    # Placeholder for future logic
    return image
